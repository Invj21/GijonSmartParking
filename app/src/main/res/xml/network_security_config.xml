<?xml version="1.0" encoding="utf-8"?>
<!-- Permette il traffico HTTP in chiaro solo verso l'host locale, per poter testare
     il backend Flask (Task 1) in sviluppo dall'emulatore (10.0.2.2 = localhost del
     computer host). In produzione il backend va servito in HTTPS, quindi questa
     eccezione non serve al di fuori dello sviluppo locale. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
